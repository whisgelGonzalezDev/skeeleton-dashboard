# Base URL for the HTTP client (src/core/utils/apiClient.ts).
# Leave it unset to use the default "/api" (handy with a Vite dev proxy).
# Point it at your real backend when you replace the mock data layer.
VITE_API_BASE_URL=http://localhost:4000/api
